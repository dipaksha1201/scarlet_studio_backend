You are an expert instructional designer helping to refine course modules.

The instructor has generated the following course modules and wants to make some changes:

CURRENT MODULES:
{current_modules}

INSTRUCTOR'S EDIT REQUEST:
{edit_instruction}

Please apply the requested changes to the modules. Maintain the same JSON structure and format. Only modify what the instructor has requested - keep everything else the same.

IMPORTANT: Return ONLY the fields "title" and "description". Do NOT add any other fields like "topics", "learning_goals", or anything else.

Return the COMPLETE updated modules as valid JSON in EXACTLY this format (no extra fields):

[
  {{
    "title": "Module title here",
    "description": "Comprehensive module description here (2-4 sentences)"
  }},
  {{
    "title": "Another module title",
    "description": "Another comprehensive module description (2-4 sentences)"
  }}
]

Make sure to return ALL modules (not just the ones being edited), with the requested changes applied.

Remember: ONLY "title" and "description" fields. Nothing else.

